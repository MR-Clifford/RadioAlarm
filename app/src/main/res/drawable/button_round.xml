<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <corners android:radius="18dp"/> <!-- if you want clear round shape then make radius size is half of your button`s height. -->
    <solid android:color="@color/primary_text"/>

    <stroke android:width="1dp"
        android:color="@color/background_color"
        />

    <padding
        android:bottom="15dp"
        android:left="10dp"
        android:right="10dp"
        android:top="15dp"/>
</shape>
